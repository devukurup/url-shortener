<div id="<%= dom_id link %>">
  <p>
    <strong>Url:</strong>
    <%= link.url %>
  </p>

  <p>
    <strong>Shortened Url:</strong>
    <%= link_to nil, "#{request.protocol}#{request.host_with_port}/#{link.slug}", target: "_blank" %>
  </p>

</div>
